# Augment UI - Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# API Configuration
# =============================================================================

# Base URL for the Augment API
# Default: http://localhost:8000
# Production example: https://api.augment.yourdomain.com
VITE_API_BASE_URL=http://localhost:8000

# =============================================================================
# Application Configuration (Optional)
# =============================================================================

# Environment identifier (development, staging, production)
# Used for conditional features and logging
# VITE_ENVIRONMENT=development

# Enable debug mode (shows additional logging in console)
# VITE_DEBUG_MODE=false

# Allowed hosts for Vite dev server (comma-separated list)
# Used to configure allowedHosts in vite.config.js for development
# Example: VITE_ALLOWED_HOSTS=example.com,subdomain.example.com
# VITE_ALLOWED_HOSTS=

# =============================================================================
# LLM Configuration (Optional)
# =============================================================================

# Default LLM provider to use (overrides backend default)
# Available providers: openai, claude, gemini
# If not set, will use the backend's default provider
# VITE_DEFAULT_LLM_PROVIDER=claude

# Default LLM model to use (must match the provider)
# IMPORTANT: Use the exact model name as returned by your backend API
# Examples for available models:
#   OpenAI: gpt-5, gpt-5-mini, gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4
#   Claude: claude-opus-4-1, claude-sonnet-4-0, claude-3-5-sonnet-20241022, claude-3-5-haiku-latest
#   Gemini: gemini-2.5-pro, gemini-2.5-flash, gemini-2.0-flash
# If not set, will use the backend's default model for the provider
# VITE_DEFAULT_LLM_MODEL=claude-3-5-sonnet-20241022

# =============================================================================
# Feature Flags (Optional)
# =============================================================================

# Enable/disable A/B testing feature
# VITE_FEATURE_AB_TESTING=true

# Enable/disable prompt viewer
# VITE_FEATURE_PROMPT_VIEWER=true

# =============================================================================
# Session Configuration (Optional)
# =============================================================================

# Session timeout in milliseconds (default: 30 minutes)
# VITE_SESSION_TIMEOUT=1800000

# =============================================================================
# Logging Configuration (Optional)
# =============================================================================

# External logging service endpoint (e.g., Sentry, LogRocket)
# VITE_LOG_ENDPOINT=

# Log level (error, warn, info, debug)
# VITE_LOG_LEVEL=info

# =============================================================================
# Notes
# =============================================================================
# 
# - All environment variables must be prefixed with VITE_ to be exposed to the app
# - Changes to .env require restarting the dev server (npm run dev)
# - Never commit .env files with actual credentials to git
# - .env files are ignored by default in .gitignore
#

